# ==============================================================================
# LucentFlow — Docker environment template
# Copy to `.env` in this directory and adjust. Convention: mainnet.base.org uses built-in safe pacing;
# .env indexer tuning applies only when LUCENTFLOW_CHAIN_RPC_URL is NOT the official Base public host.
# ==============================================================================

# --- Database ---
POSTGRES_DB="lucentflow"
POSTGRES_USER="admin"
POSTGRES_PASSWORD=lucentflow_pwd

# --- Base L2 RPC ---
LUCENTFLOW_CHAIN_RPC_URL=https://mainnet.base.org
LUCENTFLOW_CHAIN_RPC_BACKUP_URL=https://mainnet.base.org

# --- Basescan (required for deep funding traces) ---
BASESCAN_API_KEY=""
BASESCAN_BASE_URL=https://api-v2.basescan.org/api

# --- Indexer ---
LUCENTFLOW_INDEXER_START_BLOCK=44100000

# --- Observability (com.lucentflow package: INFO=quiet steady state, DEBUG=per-block [PERF] traces) ---
LUCENTFLOW_LOGGING_LEVEL_COM_LUCENTFLOW=INFO

# --- Optional ---
PROXY_HOST=""
PROXY_PORT=""
TELEGRAM_BOT_TOKEN=""
TELEGRAM_CHAT_ID=""
ALERT_MIN_SCORE=70
SPRING_PROFILES_ACTIVE="docker"
JAVA_OPTS="-XX:+UseZGC -XX:+ZGenerational -Xms512m -Xmx2g"

# ==============================================================================
# Professional RPC tuning (only when LUCENTFLOW_CHAIN_RPC_URL is NOT mainnet.base.org)
# Example: Alchemy — set URL, point backup at official public RPC, then uncomment and tune:
# ==============================================================================
LUCENTFLOW_INDEXER_POLLING_INTERVAL_MS=800
LUCENTFLOW_INDEXER_MAX_BATCH_SIZE=1500
LUCENTFLOW_INDEXER_MAX_CONCURRENCY=6
LUCENTFLOW_INDEXER_CATCHUP_BOOST_ENABLED=false
LUCENTFLOW_INDEXER_COOLDOWN_WINDOW_MS=60000
LUCENTFLOW_INDEXER_COOLDOWN_POLLING_INTERVAL_MS=15000
LUCENTFLOW_INDEXER_COOLDOWN_MAX_BATCH_SIZE=80
LUCENTFLOW_CHAIN_PROFESSIONAL_INTER_BATCH_SLEEP_MS=1500
LUCENTFLOW_INDEXER_SCHEDULER_TICK_MS=200

# LUCENTFLOW_ADMIN_API_KEY=
# Fallback only; live daily cap is projects.daily_request_quota (BUILDER=2000)
# LUCENTFLOW_API_DAILY_REQUEST_QUOTA=2000
# LUCENTFLOW_API_RATE_LIMIT_PER_MINUTE=120
# LUCENTFLOW_API_PUBLIC_RATE_LIMIT_PER_MINUTE=60
# LUCENTFLOW_API_RISK_SCORE_MAX_CONCURRENT=4
# LUCENTFLOW_API_RISK_SCORE_TIMEOUT_MS=8000
# LUCENTFLOW_API_RISK_SCORE_CACHE_TTL_MS=60000
# LUCENTFLOW_API_RISK_SCORE_CACHE_MAX_SIZE=256
# Forensic JSON/CSV export hard cap (SQL LIMIT; never unlimited)
# LUCENTFLOW_API_FORENSICS_EXPORT_MAX_ROWS=10000
# Global webhook HMAC fallback (per-project secrets preferred via Admin API webhookSecret)
# LUCENTFLOW_WEBHOOK_URL=
# LUCENTFLOW_WEBHOOK_SECRET_TOKEN=
# LUCENTFLOW_WEBHOOK_BULKHEAD_PERMITS=50
# LUCENTFLOW_WEBHOOK_BULKHEAD_ACQUIRE_TIMEOUT_MS=10000
# LUCENTFLOW_DISCORD_WEBHOOK_URL=
# LUCENTFLOW_RUNTIME_ENABLE_INDEXER=true
# LUCENTFLOW_RUNTIME_ENABLE_ANALYZER=true
# Skip rug enrich / genesis deep-trace for lower-risk whales when block lag exceeds this (default 500)
# LUCENTFLOW_ANALYZER_CATCH_UP_LAG_BLOCKS=500
# LUCENTFLOW_RUNTIME_ENABLE_API=true
# LUCENTFLOW_ORACLE_ENABLED=true
# LUCENTFLOW_NEO4J_ENABLED=false
# LUCENTFLOW_NEO4J_URI=bolt://localhost:7687
# LUCENTFLOW_NEO4J_PASSWORD=lucentflow
