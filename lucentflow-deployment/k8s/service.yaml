apiVersion: v1
kind: Service
metadata:
  name: lucentflow-api
  labels:
    app: lucentflow
spec:
  type: ClusterIP
  selector:
    app: lucentflow
    component: api
  ports:
    - name: http
      port: 8080
      targetPort: 8080
